name: 基金涨跌推送

# Schedule removed to suspend automatic runs. Use manual dispatch to run this workflow when needed.
on:
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4.1.1
      - uses: actions/setup-python@v5.1.0
        with:
          python-version: '3.11'
      - run: pip install requests -q
      - run: python fund_monitor.py
        env:
          PUSHPLUS_TOKEN: e8ab57d64b72420aa7ad6254973ef236
          FUND_CODES: "014611,161903,006533,320007"
